{
  "buildCommand": "bun run build",
  "installCommand": "bun install",
  "outputDirectory": "build/client",
  "bunVersion": "1.x",
  "includeFiles": [
    "bin/**",
    "build/**"
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/server" }
  ]
}
